<!DOCTYPE html>
<html>
    <head>
        <title>BIL402-HW3</title>
        <style>
            @import url(https://fonts.googleapis.com/css?family=Roboto:300);

            .container {
            width: 400px;
            padding: 8% 0 0;
            margin: auto;
            }

            .form {
            position: relative;
            z-index: 1;
            background: #FFFFFF;
            max-width: 400px;
            margin: 0 auto 100px;
            padding: 45px;
            text-align: center;
            box-shadow: 0 0 20px 0 rgba(0, 0, 0, 0.2), 0 5px 5px 0 rgba(0, 0, 0, 0.24);
            }

            .form textarea {
            resize: none;
            width: 100%;
            height: 60px; 
            box-sizing: border-box;
            border:solid 1px grey;
            }

            .form input {
            font-family: "Roboto", sans-serif;
            text-transform: uppercase;
            outline: 0;
            background: #4CAF50;
            width: 100%;
            border: 0;
            padding: 15px;
            color: #FFFFFF;
            font-size: 14px;
            -webkit-transition: all 0.3 ease;
            transition: all 0.3 ease;
            cursor: pointer;
            }

            .form input:hover,.form input:active,.form input:focus {
            background: #43A047;
            }

            body {
            background: #76b852; /* fallback for old browsers */
            background: rgb(141,194,111);
            background: linear-gradient(90deg, rgba(141,194,111,1) 0%, rgba(118,184,82,1) 50%);
            font-family: "Roboto", sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;      
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="form">
                <h1>BIL402-HW3</h1>
                <p>Girililen dizide üç sayının toplamından büyük eleman varsa çıktı 'True' olacaktır.</p>
                <form action="compute" method="post">
                <textarea name="arr" placeholder="Kontrol edilecek dizi."></textarea>
                <textarea name="first" placeholder="Bir sayı giriniz."></textarea>
                <textarea name="second" placeholder="Bir sayı giriniz."></textarea>
                <textarea name="last" placeholder="Bir sayı giriniz."></textarea>
                <input type="submit" value="Hesapla"/>
                {{#result}}
                <p>{{result}}</p>
                {{/result}}
                </form>
            </div>
        </div>
    </body>
</html>